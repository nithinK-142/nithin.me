import { InfiniteMovingCards } from "@/app/(routes)/test/moving-card/infinite-moving-cards";

const TestPage = () => {
  return <InfiniteMovingCards direction="right" speed="slow" />;
};

export default TestPage;
